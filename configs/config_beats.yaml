# Generated by scripts/train_beats_mixup_lr_sweep_trainscp_ep10.sh
# lr=0.0001, weight_decay=0.005, mixup_alpha=0.35, num_epochs=10
data:
  train_txt: /workspace/data/train.scp
  sample_rate: 16000
  batch_size: 16
  num_workers: 4
  pin_memory: true
model:
  type: beats
  checkpoint_path: beats/BEATs_iter3_plus_AS2M.pt
  num_classes: 531
  feature_dim: 768
  dropout: 0.1
  arc_margin:
    s: 30.0
    m: 0.5
    easy_margin: false
finetuning:
  strategy: full
training:
  device: cuda
  gpu_id:
  - 0
  num_epochs: 10
  learning_rate: 0.0001
  weight_decay: 0.005
  mixup_alpha: 0.35
  save_interval: 1
  max_grad_norm: 1.0
  scheduler:
    step_size: 5
    gamma: 0.5
logging:
  tag: beats_full_trainscp_lr1e-4_wd5e-3_mix035_ep10
  log_dir: runs/sweep_mixup_lr_train_scp_ep10
  save_dir: checkpoints_sweep_mixup_lr_train_scp_ep10/beats_full
  log_interval: 10
checkpoint:
  resume_epoch: null
validation:
  enabled: true
  scp_path: /workspace/data/test_dev.scp
  train_scp_path: /workspace/data/train.scp
  score_method: knn
  top_k: 1
  n_clusters: 10
  batch_size: 32
  num_workers: 4
  parallel_machines: false
  augmentation:
    use_adasyn: false
    use_mixup: false
    mixup_alpha: 0.25
    skip_adasyn_machines:
    - fan
